[project]
name = "api-escolar"
version = "0.1.0"
description = "API REST para alumnos y profesores en memoria"
requires-python = ">=3.10"
dependencies = [
  "fastapi==0.115.12",
  "uvicorn[standard]==0.34.0",
  "httpx==0.28.1"
]

[tool.uvicorn]
host = "0.0.0.0"
port = 8000
reload = true
